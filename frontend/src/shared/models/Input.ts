// allows letters (upper and lower case), numbers, some symbols
export const inputWithSymbolRegex = /^[a-zA-Z0-9@!_<>^~]+$/;

// allows letters (upper and lower case) and spaces
export const inputWithSpaceRegex = /^[a-zA-Z\s]+$/;

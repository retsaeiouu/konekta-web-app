{
  "name": "api",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=dev npx tsx watch --clear-screen=false --exclude \"./node_modules/**/*\" ./src/index.ts",
    "up-compose": "docker-compose --env-file ../.env.${NODE_ENV:-dev} up -d",
    "down-compose": "docker-compose --env-file ../.env.${NODE_ENV:-dev} down",
    "db-migrate": "npx dotenv -e ../.env.${NODE_ENV:-dev} -- npx prisma migrate dev"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "dotenv": "^16.4.7",
    "express": "^5.0.1",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.10.7",
    "dotenv-cli": "^8.0.0",
    "prisma": "^6.2.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
